<main>
    <meta id="this-isnt-the-word-you-cheater" data-the-word="{{word}}" />
    <meta id="wurmble-no" data-the-day="{{wurmbleno}}" />
    <div class="stuff">
        <div>
            <div class="loser-banner" id="loser-banner" style="opacity: 0;">
                <div id="loser-ting">
                    {{word}}
                </div>
                <div id="winner-ting">
                    {{word}}
                </div>
            </div>
            <div class="grid">
                {{#each (range 5)}}
                {{>square sq=this}}
                {{/each}}
            </div>
        </div>
        {{>keyboard}}
    </div>
</main>